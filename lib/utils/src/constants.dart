import 'package:flutter/material.dart';

class Constants {
  static const String isFirstTime = 'isFirstTime';
  static const String loggedIn = 'loggedIn';
  static const Icon userIcon = Icon(Icons.group_add);
  static const Icon viewIcon = Icon(Icons.remove_red_eye_outlined);
}
